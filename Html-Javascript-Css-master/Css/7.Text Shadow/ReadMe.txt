CSS3 allows us to add shadow to the text of any HTML element using the text-shadow property.

Ex: text-shadow: 2px 2px 2px balck;

horizontal offset:
Positive value creates a shadow to the right.
Negative value creates the shadow to the left

vertical offset:
Positive value creates a shadow to the left.
Negative value creates the shadow to the right.

blur distance:
An optional value which decides the percentage of blur effect takes only positive value.

color:

An optional value which specifies the shadow color.